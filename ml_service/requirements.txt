--extra-index-url https://download.pytorch.org/whl/xpu
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.45.0
pillow>=10.0.0
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
websockets>=10.4
python-multipart>=0.0.6
pydantic>=2.0.0
numpy>=1.24.0
scipy>=1.11.0
gunicorn>=21.2.0

# OpenVINO acceleration (Intel Arc iGPU / NPU / CPU)
# 2-3x speedup on Intel hardware. Set ML_USE_OPENVINO=true to enable.
optimum[openvino]>=1.20.0
openvino>=2025.0

# ONNX Runtime (alternative CPU acceleration)
# 1.5-2.5x CPU speedup. Set ML_USE_ONNX=true to enable.
# optimum>=1.17.0
# onnxruntime>=1.17.0

# SAM 2.1 Segmentation
# Pixel-perfect trunk segmentation. Set ML_ENABLE_SAM=true to enable.
# Option A: HuggingFace transformers (already included above, uses Sam2Model)
# Option B: Native Facebook SAM2 package (requires Python>=3.10, PyTorch>=2.5.1)
sam2

# Ellipse fitting for trunk cross-section
scikit-image>=0.21.0

# Phase 3+: DA3 Depth Anything 3
# WARNING: DA3 officially requires CUDA. CPU support untested.
# xformers
# depth-anything-3