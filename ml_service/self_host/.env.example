# ============================================================
# Tree ML Service — 自架環境變數設定
# ============================================================
# 複製此檔案並填入你的設定:
#   cp .env.example .env
#   然後編輯 .env
# ============================================================

# [必須] ML API Key — 保護 ML 端點的認證金鑰
# 建議使用隨機字串，例如:
#   openssl rand -hex 32
# 然後在 Flutter App 的 ML 服務設定中填入相同的 Key
ML_API_KEY=

# [選填] 允許的 CORS 來源 (逗號分隔)
# 預設允許: localhost 和 Render backend
# ML_CORS_ORIGINS=http://localhost:3000,https://tree-app-backend-prod.onrender.com

# [選填] 服務埠號 (預設 8000)
# PORT=8000

# [選填] 每小時每 IP 最大請求數 (預設 30)
# ML_RATE_LIMIT=30

# [選填] 啟用 FastAPI /docs 介面 (開發用)
# ML_DEBUG=true
